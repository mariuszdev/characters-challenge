import {connect} from 'react-redux';

import CharactersListItem from '../components/characters/CharactersListItem';

const mapStateToProps = (state) => ({
});

export default connect(mapStateToProps)(CharactersListItem);
